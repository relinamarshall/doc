---
layout:
  title:
    visible: false
  description:
    visible: false
  tableOfContents:
    visible: true
  outline:
    visible: true
  pagination:
    visible: true
---

# 访问和联结方法

优化器必须确定如何来访问你的SQL语句所需要的数据。你构造SQL语句，而优化器则在硬解析的过程中算出哪些运算能够尽可能高效地提供数据。主要根据统计信息，优化器将会计算各个备选项首先访问数据然后进行多表联结以获取最终数据集的成本。你对优化器可能会考虑的不同访问和联结方法了解得越多，就越能够构造出恰当的SQL语句来帮助优化器作出最好的选择。并且，当优化器所选择的运算不能满足你的性能需求时，你可以更准确地来确定哪些运算更适合 提供你想要的响应时间。

在SQL语句的表达式和条件均评估完毕，以及所有可能有助于更准确地生成执行计划的查询转换都进行完以后，生成执行计划的下一个步骤就是利用优化器来确定访问数据的最好方法。一般来说，总共只有两种基本的数据访问途径:**全扫描**或者**索引扫描**。在全扫描(顺便说一下，可能是**全表扫描**，也可能是**快速全索引扫描**)过程中，多个块被读入到一个I0运算中。**索引扫描**首先扫描索引叶子块以取得特定的行id，然后利用这些行id来访问父表取得实际的行数据。这些访问都是通过单块读取来完成的。如果在数据表访问的步骤之后还需要进一步的数据选，这些行将会在被包含在最终结果集之前通过筛选。&#x20;

某个表所选定的访问方法被用来确定最终计划中所使用的表联结方法和联结顺序。因此，如果为某个表选择的访问方法并不是最优的，整个执行计划不完美的可能性也就相应更大。统计信息在优化器准确确定最优方法的过程中扮演了关键的角色。与典型的统计信息一起,优化器将利用你的查询来算出你需要多少数据以及哪种访问方法能够尽可能快地提供数据。查询中的每个表都会首先被独立评估以确定它的最佳访问途径。下面将详细讲解各种访问方法。

## 全扫描访问方法

当对一个对象进行全扫描时，与该对象相关的所有数据块都必须取出并进行处理，以确定块中所包含的数据行是否是你的查询所需要的。请记住Oracle必须将整个数据块读取到内存中以取得这个块中所存储的数据行的数据。**因此，当发生全扫描时，实际上优化器(还有你)需要考虑两件事:必须读取多少个数据块以及每个数据块中有多少数据将被舍弃**。此时此刻我想要让你理解的是，确定全扫描是否是正确选择并不仅仅基于查询将会返回多少行。有很多已发表的“经验法则”会这样说:“如果你的查询将会取出表中多于x%的数据行，则应该选择全扫描。”除了这些经验法则(ROT，Rule Of Thumb=ROT)以外还有很多要考虑的，不要被这些法则束缚着而忽略了为了做出正确选择而需要考虑的其他方面。我并不是说经验法则背后的原理在逻辑上没有意义，而只是说需要考虑的事情并不仅限于此。当一个查询需要返回表中绝大多数的数据行时，选择使用全扫描的可能性当然也是很高的,但这个普遍规则的问题在于所返回行数的比例在某种程度上是任意的。在这些年中，这个法则在不同的书籍、论文以及论坛中都出现过，而这个比例从20%\~70%都有。为什么它要变呢?

### 如何选择全扫描操作

全表扫描选择规则所存在的问题，它不仅仅是与数据行有关，还与数据块以及丢弃有关。将所有这些方面的信息集合起来，就会得出全表扫描是否有意义的结论，即使返回的数据行百分比可能很小。另一方面，即使返回的数据行百分比很高也有可能不应该选择全表扫描。看下面例子，看看即使查询仅需要返回很小比例的数据行时，优化器为何也有可能选择全表扫描执行计划。首先，创建了两个同样的具有10000行数据的表。然后，给出了每张表对于同一个查询的执行计划，注意为什么即使查询仅返回100行(只有总数据量的1%)也会选择全表扫执行计划。

> 创建两个测试表

<pre class="language-sql" data-title="SQLPlus" data-line-numbers><code class="lang-sql">create table t1 as
select trunc((rownum-1)/100) id,rpad(rownum,100) t_pad
from DBA_SOURCE where rownum &#x3C;= 10000;

create index t1_idx1 on t1(id);

--收集表t1统计信息
<a data-footnote-ref href="#user-content-fn-1">exec</a> <a data-footnote-ref href="#user-content-fn-2">dbms_stats.gather_table_stats</a>(<a data-footnote-ref href="#user-content-fn-3">user</a>,<a data-footnote-ref href="#user-content-fn-4">'t1'</a>,<a data-footnote-ref href="#user-content-fn-5">method_opt=>'FOR ALL COLUMNS SIZE 1'</a>,<a data-footnote-ref href="#user-content-fn-6">cascade=>TRUE</a>);

create table t2 as
select MOD(rownum,100) id,rpad(rownum,100) t_pad
from DBA_SOURCE where rownum &#x3C;= 10000;

create index t2_idx1 on t2(id);

--收集表t2统计信息
exec dbms_stats.gather_table_stats(user,'t2',method_opt=>'FOR ALL COLUMNS SIZE 1',cascade=>TRUE);
</code></pre>

每个表都有10000行数据。两个表中的id列对于0\~99的每个值都有100行。因此，从数据内容的角度来说,这两张表是一样的。但是,注意对于T1,id列是通过表达式`trunc((rownum-1)/100)`来计算的，而T2中的id列则使用`mod(rownum,100)`计算。下图展示出了表中的数据块是如何进行物理存储的。

<figure><img src="../.gitbook/assets/image (21) (1).png" alt=""><figcaption><p>随机存储与顺序存储数据行值对比图</p></figcaption></figure>

对于每个表中的10000行，如果你查询单个值`(where id=1)`，你知道自己将重新得到100行，这只是总行数的1%。对于这么小的百分比来说，你也将预期优化器会选择使用id列的索引来访问数据行，对吗?这看上去似乎是很符合逻辑的结论，但此时正应该去了解一下数据是如何存储的。如果数据是顺序存储的，大多数的`id=1`的行在物理上仅存储于几个数据块中，如本例中的T1那样，那么这个结论是对的。

{% code lineNumbers="true" %}
```sql
explain plan for
select * from t1 where id = 1;

select * from table(DBMS_XPLAN.display());

PLAN_TABLE_OUTPUT                                                                      |
---------------------------------------------------------------------------------------+
Plan hash value: 2623418078                                                            |
---------------------------------------------------------------------------------------|
| Id  | Operation                   | Name    | Rows  | Bytes | Cost (%CPU)| Time     ||
---------------------------------------------------------------------------------------|
|   0 | SELECT STATEMENT            |         |   100 | 10400 |     3   (0)| 00:00:01 ||
|   1 |  TABLE ACCESS BY INDEX ROWID| T1      |   100 | 10400 |     3   (0)| 00:00:01 ||
|*  2 |   INDEX RANGE SCAN          | T1_IDX1 |   100 |       |     1   (0)| 00:00:01 ||
---------------------------------------------------------------------------------------|
Predicate Information (identified by operation id):                                    |
---------------------------------------------------                                    |
   2 - access("ID"=1)                                                                  |
```
{% endcode %}

那么，你是否预期对于T2的查询也是一样的呢?因为它同样也返回100行数据。正如下面代码中的解释计划所看到的，事实并不是这样。

{% code lineNumbers="true" %}
```sql
explain plan for
select * from t2 where id = 1;

select * from table(DBMS_XPLAN.display());

PLAN_TABLE_OUTPUT                                                         |
--------------------------------------------------------------------------+
Plan hash value: 1513984157                                               |
--------------------------------------------------------------------------|
| Id  | Operation         | Name | Rows  | Bytes | Cost (%CPU)| Time     ||
--------------------------------------------------------------------------|
|   0 | SELECT STATEMENT  |      |   100 | 10400 |    46   (0)| 00:00:01 ||
|*  1 |  TABLE ACCESS FULL| T2   |   100 | 10400 |    46   (0)| 00:00:01 ||
--------------------------------------------------------------------------|
Predicate Information (identified by operation id):                       |
---------------------------------------------------                       |
   1 - filter("ID"=1)                                                     |
```
{% endcode %}

为什么优化器没有为这两个查询选择同样的解释计划呢?这是因为在这两张表中数据的存储方式是不同的。对表T1的查询Oracle仅需要访问很少的几个数据块来取得查询所需的100行数据。因此，使用索引就是最具吸引力的选择。但是，由于表T2的数据行在物理上是零散存储在该表的所有数据块中，对于它的查询就需要读取几乎所有数据块来获取同样的100行数据。优化器计算出使用索引来读取表中每一个数据块的时间可能比直接使用全表扫描读取所有数据块,然后直接将每个块中不需要的数据舍弃掉所花的时间还要长。从索引中取出这些行大约需要访问200个数据块。在下一节中讨论索引扫描的时候我将说明为什么会是200个。因此，对表T2的查询将会使用全表访问运算而不是使用索引。

这个例子向你展示了基于数据存储方式的不同优化器的执行计划选择也可能会不同。然而知道这一点并不一定会使你写SQL查询时有所改变，它可以影响的是如何确定查询性是否能满足服务水平协议**(Service Level Agreement,SLA)**。如果你总是看到全表扫描运算的使用，你可能就会想到需要改变查询或者给它点儿提示来强制使用索引。但从长远来看这样做可能会导致性能更差。如果不理解数据是如何存储的,在查询执行的时候对于应该发生什么你就可能会做出差劲的决策。

### 全扫描与舍弃

请记住，全扫描是否为高效的选择取决于需要访问的数据块个数以及最终的结果集行数。如上例所示，数据的存储方式在决策过程中扮演了重要的角色。此外，全扫描是否为高效选择的另一个关键因素是舍弃。舍弃的行是那些通过一个选谓语来进行验证,被证明是不符合筛选条件 后从最终的结果集中剔除的数据行。

在上一个例子中，全表扫描运算将必须检查表中所有的10000行数据并舍弃其中的9900行以得出最终的100行结果集。对于每一行所做的检查就仅仅是选谓语`id=1`。为了执行这个筛选，每一次检查都会使用CPU。这也就意味着尽管所需要访问的数据块数目是有限的，为了完成每一行的检查选仍然需要花费相当多的CPU资源。CPU的使用将会归入到全扫描成本中。&#x20;

所访问数据块数目以及舍弃的数量越大，全表扫描的成本也就越高。下列代码一个简单的用以展示上面例子中表T2的数据行以及数据块的查询。基于所列出的数据块数目，全扫描大约需要访问164个数据块。

> 表T1和表T2的数据行和数据块统计信息

{% code lineNumbers="true" %}
```sql
SELECT TABLE_NAME,NUM_ROWS,BLOCKS 
FROM ALL_TABLES WHERE TABLE_NAME='T2'
```
{% endcode %}

随着时间的推移，新的行被插入到表中使得表变得更大，舍弃这么多数据行的成本也不断增加，到一定程度时优化器将会切换到使用索引扫描运算。优化器决定进行切换的点并不一定是你实现最佳性能的点。你可以通过使用提示来强制优化器使用索引,以测试在哪个点上使用索引更有意义。如果优化器没有选择使用，你可以考虑使用提示或者SQL概要文件来帮助它选用，第16章将会讲述使用提示及概要文件。

### 全扫描与多块读取

关于全扫描，你还需要知道的是数据块是如何读取的。全扫描运算将会进行多块读取。也就是说一个单独的IO调用将会请求多个块而不是仅仅一个。所请求的数据块数目是可变的，实际上可以是从一个到`db file_multiblock_read count`参数所指定的数目范围之间的任意个。例如，如果这个参数设定为16，而表中有160个块，可以只通过10次调用就可以来获取所有的数据块。&#x20;

我说可以仅通过10次调用就可以来获取是因为下面这些关于多块读取调用的限制。除非已经读取了所有的数据块，否则Oracle将会读取`db file_multiblock_read count`中指定数目的数据块。

* Oracle不得不读取超过一定边界范围的数据块。在这种情况下，Oracle将会在一次调用中读取直到边界范围的数据块，然后发起另一次调用来读取剩余的块。
* 如果一个数据块已经在缓冲区缓存中，那么它将会作为多块读取的一部分重新读取一遍。Oracle将会只读取那些尚在内存中的数据块，然后发起另一个读取调用来跳过这些数据块来读取剩下的。这也就意味着一个多块读取可能一次只读取一个块。例如，让我们假设多块读取的计数是16个，需要读取的是编号为1\~16的块。如果偶数编号的块已经在缓冲区缓存中了，那么在这个范围内的每一个奇数编号的块就需要进行一次单独的单块读取，在这个例子中，需要进行8次读取调用—每次读取一个该范围内的不在缓冲区缓存中的数据块。
* 超出了操作系统限制的多块读取大小。这取决于你的操作系统，因此是可变的。

### 全扫描与高水位线

关于全表扫描需要注意的最后一点是当对扫描进行多块读取调用时,Oracle将最多读取到位于表中高水位线的数据块。高水位线标出了表中最后一块有数据写入的数据块。当数据行插入一张表中的时候，就会为其分配数据块并将数据行放到其中。下图展示了当对表进行了大数据插入后的情景。

<figure><img src="../.gitbook/assets/image (9) (1).png" alt=""><figcaption><p>数据块被分配给一张具有用+号标出的数据行的表</p></figcaption></figure>

在经过一系列常规操作之后，数据行被从表中删除。下图所示为从数据表中删除掉很多行之后麦中的情景。

<figure><img src="../.gitbook/assets/image (1) (1) (1) (1).png" alt=""><figcaption><p>数据行被删除后数据块中的情景，高水位线保持不变</p></figcaption></figure>

即使几乎所有数据行都被删除了并且一些块实际上已经完全变成空的了,高水位线还是保持不变。当进行全扫描运算的时候，到高水位线为止的所有数据块都将被读取并扫描，即使它们是空的。这就意味着许多实际上不需要读取的空数据块也被读取了。下列代码清给出了为什么即使表中所有行都被删除掉了，高水位线还是保持不变的一个例子。

> 高水位线

{% code lineNumbers="true" %}
```sql
--列出有多少数据块
select blocks from USER_SEGMENTS where SEGMENT_NAME = 'T2';
--列出有多少块包含数据
select count(distinct (dbms_rowid.rowid_block_number(rowid))) block_ct from t2;
--列出该表的最低和最高块号
select
	min(dbms_rowid.rowid_block_number(rowid)) min_blk,
	max(dbms_rowid.rowid_block_number(rowid)) max_blk
from t2;
```
{% endcode %}

{% code title="space_usage.sql" lineNumbers="true" %}
```sql
--创建存储过程查询空间使用情况
CREATE OR REPLACE PROCEDURE GET_SPACE_USAGE(p_table_name IN VARCHAR2)
IS
    l_fs1_bytes          NUMBER;
    l_fs2_bytes          NUMBER;
    l_fs3_bytes          NUMBER;
    l_fs4_bytes          NUMBER;
    l_fs1_blocks         NUMBER;
    l_fs2_blocks         NUMBER;
    l_fs3_blocks         NUMBER;
    l_fs4_blocks         NUMBER;
    l_full_bytes         NUMBER;
    l_full_blocks        NUMBER;
    l_unformatted_bytes NUMBER;
    l_unformatted_blocks NUMBER;
BEGIN
    DBMS_SPACE.SPACE_USAGE(
    	segment_owner	    => user,
        segment_name        => p_table_name,
        segment_type        => 'TABLE',
        fs1_bytes           => l_fs1_bytes,
        fs1_blocks          => l_fs1_blocks,
        fs2_bytes           => l_fs2_bytes,
        fs2_blocks          => l_fs2_blocks,
        fs3_bytes           => l_fs3_bytes,
        fs3_blocks          => l_fs3_blocks,
        fs4_bytes           => l_fs4_bytes,
        fs4_blocks          => l_fs4_blocks,
        full_bytes          => l_full_bytes,
        full_blocks         => l_full_blocks,
        unformatted_bytes   => l_unformatted_bytes,
        unformatted_blocks  => l_unformatted_blocks
    );
    DBMS_OUTPUT.PUT_LINE('0-25% Free  =' || l_fs1_blocks || ' Blocks =' || l_fs1_bytes || ' Bytes');
    DBMS_OUTPUT.PUT_LINE('25-50% Free =' || l_fs2_blocks || ' Blocks =' || l_fs2_bytes || ' Bytes');
    DBMS_OUTPUT.PUT_LINE('50-75% Free =' || l_fs3_blocks || ' Blocks =' || l_fs3_bytes || ' Bytes');
    DBMS_OUTPUT.PUT_LINE('75-100% Free=' || l_fs4_blocks || ' Blocks =' || l_fs4_bytes || ' Bytes');
    DBMS_OUTPUT.PUT_LINE('Full Blocks =' || l_full_blocks || ' Blocks =' || l_full_bytes || ' Bytes');
    EXCEPTION
    WHEN OTHERS THEN
        DBMS_OUTPUT.PUT_LINE('An error occurred: ' || SQLERRM);
END;
```
{% endcode %}

{% code lineNumbers="true" %}
```sql
--检查表中的空间使用情况
CALL GET_SPACE_USAGE('T2');
--大多数块都是full全表扫描,必须读取所有的块(152总数)
0-25% Free  =0 Blocks =0 Bytes
25-50% Free =0 Blocks =0 Bytes
50-75% Free =0 Blocks =0 Bytes
75-100% Free=0 Blocks =0 Bytes
Full Blocks =152 Blocks =1245184 Bytes

--删除全表
DELETE FROM T2;
--检查表中的空间使用情况
CALL GET_SPACE_USAGE('T2');
0-25% Free  =0 Blocks =0 Bytes
25-50% Free =0 Blocks =0 Bytes
50-75% Free =0 Blocks =0 Bytes
75-100% Free=152 Blocks =1245184 Bytes
Full Blocks =0 Blocks =0 Bytes
```
{% endcode %}

{% code title="SQLPlus" lineNumbers="true" %}
```sql
--请注意，块现在是空闲的，但仍然消耗相同的空间。全表扫描仍然会读取156块(表有0行)。
select blocks from USER_SEGMENTS where SEGMENT_NAME = 'T2';
--现在有数据的数据块为0
select count(distinct (dbms_rowid.rowid_block_number(rowid))) block_ct from t2;

--执行一次全表扫描，并注意一致性读取(逻辑块读取)
set autotrace traceonly;
select * from t2;

执行计划
----------------------------------------------------------
Plan hash value: 1513984157
--------------------------------------------------------------------------
| Id  | Operation         | Name | Rows  | Bytes | Cost (%CPU)| Time     |
--------------------------------------------------------------------------
|   0 | SELECT STATEMENT  |      | 10000 |  1015K|    46   (0)| 00:00:01 |
|   1 |  TABLE ACCESS FULL| T2   | 10000 |  1015K|    46   (0)| 00:00:01 |
--------------------------------------------------------------------------
统计信息
----------------------------------------------------------
          0  recursive calls
          0  db block gets
        156  consistent gets
          0  physical reads
          0  redo size
        405  bytes sent via SQL*Net to client
        512  bytes received via SQL*Net from client
          1  SQL*Net roundtrips to/from client
          0  sorts (memory)
          0  sorts (disk)
          0  rows processed
```
{% endcode %}

<pre class="language-sql" data-line-numbers><code class="lang-sql"><strong>TRUNCATE TABLE T2;
</strong>CALL GET_SPACE_USAGE('T2');
0-25% Free  =0 Blocks =0 Bytes
25-50% Free =0 Blocks =0 Bytes
50-75% Free =0 Blocks =0 Bytes
75-100% Free=0 Blocks =0 Bytes
Full Blocks =0 Blocks =0 Bytes

--注意,空间已经分配了块数的块(表有0行,所有空间恢复)
select blocks from USER_SEGMENTS where SEGMENT_NAME = 'T2';
BLOCKS|
------+
     8|

set autotrace traceonly;
select * from t2;
执行计划
----------------------------------------------------------
Plan hash value: 1513984157
--------------------------------------------------------------------------
| Id  | Operation         | Name | Rows  | Bytes | Cost (%CPU)| Time     |
--------------------------------------------------------------------------
|   0 | SELECT STATEMENT  |      | 10000 |  1015K|    46   (0)| 00:00:01 |
|   1 |  TABLE ACCESS FULL| T2   | 10000 |  1015K|    46   (0)| 00:00:01 |
--------------------------------------------------------------------------
统计信息
----------------------------------------------------------
          1  recursive calls
          0  db block gets
          3  consistent gets
          0  physical reads
          0  redo size
        405  bytes sent via SQL*Net to client
        512  bytes received via SQL*Net from client
          1  SQL*Net roundtrips to/from client
          0  sorts (memory)
          0  sorts (disk)
          0  rows processed

set autotrace off;
</code></pre>

希望这个例子能向你说明即使全表扫描是“正确地”执行了计划选择，读取额外的空块所带来的成本也可能严重降低性能。对于频繁加载和清除的表(使用DELETE而不是TRUNCATE)，你可能发现响应时间会变慢。这对于用来进行提取/转换/加载(ETL)或者任何形式的加载/处理/清除操作的表来说是经常发生的。既然知道了全表扫描行为都会受到哪些影响，你可以更容易地来诊断并修正这方面的性能问题。

## 索引扫描访问方法

如果你有一本关于美国总统的书并需要查找吉米·卡特的信息，你可以从第一页开始逐行查看每一页直到书中关于卡特的那部分。但进行这样的浏览将会花费大量的时间，因此你可能会发现通过书中的索引来查找卡特将会更方便。

只要你找到了页码，就可以直接翻到那一页。索引扫描运算从概念上来说就类似于书中的索引。默认的索引类型是B-树索引，我在本章中也将仅讨论这种索引。索引建立在表中的一个或多个列或者是列的表达式上，将列值和行编号(rowid)一起存储。在索引中还保存着一些其他信息，但对你来说仅需要关心列值和行编号即可。行编号是一个用来唯一标记表中行的伪列。它是物理表中行数据的内部地址，包含两个地址，其一为指向数据表中包含该行的块所存放数据文件的地址，另一个是可以直接定位到数据行自身的这一行在数据块中的地址。下列代码示出了如何将行编号解码为可读内容。

> 行编号解码

{% code lineNumbers="true" %}
```sql
SELECT 
    e.rowid,
    (
        select file_name from dba_data_files 
        where file_id = dbms_rowid.rowid_to_absolute_fno(e.rowid,'SCOTT','EMP')
    ) filen,
    dbms_rowid.rowid_block_number(e.rowid) block_no,
    dbms_rowid.rowid_row_number(e.rowid) row_no
FROM EMP e
WHERE e.ename='KING';

ROWID             |FILEN                             |BLOCK_NO|ROW_NO|
------------------+----------------------------------+--------+------+
AAAR3sAAEAAAACXAAI|F:\ORACLE\ORADATA\ORCL\USERS01.DBF|     151|     8|
```
{% endcode %}

正如你所看到的，行编号指向一个特定行的准确位置。因此当使用索引来访问数据行时，就会对谓语中所提供的访问标准进行匹配，然后使用行编号来访问指定的文件/数据块/数据行。**通过索引扫描来进行的数据块访问是通过单块读取来完成的。**当你考虑行编号是如何来使用的时候这是很有意义的。一旦读取了索引条目，就只会取出行编号所指定的那一块数据;在它被取出以 后，就仅有行编号所指定的行被访问。

**这也就是说对于通过索引扫描所获取的每一行，都至少需要访问两个数据块:至少是一个索引块和一个数据块。**如果你的最终结果集中有100行，而这100行数据是通过索引扫描取得的，则至少需要访问200个数据块。我一直在说“至少”是因为根据索引的大小，Oracle有可能首先必须访问多个索引块才能获取所需要的第一个符合条件的列值。

### 索引结构

索引具有一定的逻辑结构，如下图所示。索引包含一个或多个层次的分支块以及一个层次的叶子块。分支块保存着下一层级分支所包括的值范围信息，它会被用来在索引结构中进行搜索以获得所需的叶子块。**索引的高度是指第一个分支块(也就是根数据块)到叶子块的分支层级数目。**如前面所提到的，叶子分支包含排序后的索引对象的值和行编号如果你新建一张空表，并在其上创建索引，索引将会包含一个空块。在这种情况下，这个唯一的块既是根数据块又是叶子数据块。索引的高度是1。还有另一个被称为`blevel`的统计信息用来表示一个索引中的分支层级数。在本例中，blevel为0。

<figure><img src="../.gitbook/assets/image (7) (1) (1) (1).png" alt=""><figcaption><p>索引结构的逻辑图</p></figcaption></figure>

随着新行的插入，新的索引条目会被加入到索引块中，直到该块无法容纳更多的条目。当索引块已经填满，无法再容纳新的条目时，Oracle 会分配两个新的索引块，并将所有的索引条目加入到这两个新的叶子块中。原先指向被填满的根数据块现在会被替代为指向两个新数据块的指针。这个指针包括指向新索引块的相对数据块地址`（Relative Block Address，RBA）`和一个表示相关叶子块中最低索引值的值。Oracle 可以利用根数据块中的信息来搜索索引，以找到存有所需值的特定叶子块。

到目前为止，这个索引的高度为2，blevel为1。 随着时间的推进，更多的行插入到表中，索引条目被加入刚才创建的两个叶子数据块中。当这两个叶子块也被填满之后，Oracle将会增加一个新的叶子块并为它分配介于已填满的块与新叶子块之间的索引条目。每次一个叶子数据块填满并分裂之后,就会为这个新的叶子块增加一个新指针到根数据块中。最终,根数据块也被填满了,然后再重复将根数据块分裂为两个新的分支块。当发生这样的分裂的时候，索引的高度将会增加为3而blevel变为2。

现在，随着新的索引条目的加入，叶子数据块将会被填满并分裂，但不是添加一个新指针到根数据块中，而是将指针加入到相应的分支数据块中。最终，分支数据块也将被填满并分裂，这时候又将有一个新的索引条目加入到根数据块中。随着这些过程的不断继续,最后根数据块又会被填满并分裂，再一次增加索引的高度。请记住唯一引起索引高度增加的就是当根数据块分裂的时候。由于这一点，所有叶子数据块到根数据块的距离都是一样的。这也就是为什么你会听到用术语平衡的来形容Oracle的B-树索引。一定要确保索引的高度是保持平衡的。

为什么要阐述所有这些细节呢?理解索引结构是如何来创建和维护的将会有助于你理解各种索引扫描是怎样工作的。既然对索引是如何组织的有了一定的了解，你要准备探索不同的索引扫描是如何来遍历索引结构以获取你的查询所需的数据行的。

### 索引扫描类型

索引扫描有好几种不同的类型,但每种类型都有的共同点就是它们都必须遍历索引结构以访问匹配所搜索值的叶子索引块。首先，索引的根块将通过一次单块读取来访问。接下来的步骤就是读取分支块。根据索引高度的不同，可能需要读取一个或多个分支块。每次读取都是针对一个独立的数据块。最后，读取到了包含所需的起始索引条目的第一个叶子索引块。如果索引的高度为4，为了得到所需的叶子块，将会对4个单独的块进行读取。此时，就会读取叶子块中第一个匹配的索引值的`行编号(rowid)`，并利用这个行编号来进行一次独立的块读取以获取整个行数据所存储的表数据块。因此，在这个例子中，使用索引从表中取出一行，Oracle必须读取5个块:4个索引块和1个表数据块。

索引扫描类型包括索引范围扫描、索引唯一扫描、索引全扫描、索引跳跃扫描以及索引快速全扫描。索引的快速全扫描实际上更像是进行一次全表扫描，但它扫描的是索引结构。

在讲述不同的扫描类型之前，我需要解释一个非常重要的索引统计信息--**`聚簇因子(clustering factor)`**。索引的聚簇因子统计信息帮助优化器生成使用索引的成本信息，并且是表中建立了索引的数据排序优良度的一个度量值。回忆一下索引条目是按顺序存储的而表中的数据是随机存储的。除非特意去指定特定的顺序将数据载入到表中，否则将不能保证某一行将会被放在什么地方。例如，orders表中具有同样order\_date的数据行可能不是存放在同一个数据块中。它 们很可能是随机分散在该表的多个数据块中。

索引的聚簇因子向优化器表明了具有同样索引值的数据行是不是存放在同一个或连续的一系列数据块中，或者数据行是否被分散存放在表的多个数据块中。下图展示出了数据行在物理上可能如何存储在表的数据块中。

<figure><img src="../.gitbook/assets/image (8) (1) (1).png" alt=""><figcaption><p>随机与顺序载入的行数据对比图</p></figcaption></figure>

在表示T1表的图中，你可以看到包含值2的数据行是如何被载入到同一个数据块中去的。但是，对于表T2，值为2的行并不是载入到连续的数据块中。在这个例子中，建立在T1表中这一列上的索引将会具有较低的聚簇因子。用一个接近于表中数据块数量的较低数值来表明该表中基于索引后的数据行的排序度或聚簇度是很高的。而表T2的聚簇因子将会更高并更接近于表中的数据行数。下代码列出了这两个表的聚簇因子统计信息。

> 索引聚簇因子

{% code lineNumbers="true" %}
```sql
SELECT 
    T.TABLE_NAME || '.' || I.INDEX_NAME IDX_NAME,
    I.CLUSTERING_FACTOR ,T.BLOCKS ,T.NUM_ROWS 
FROM USER_INDEXES I,USER_TABLES T
WHERE I.TABLE_NAME = T.TABLE_NAME
AND T.TABLE_NAME IN ('T1','T2')
ORDER BY T.TABLE_NAME,I.INDEX_NAME
```
{% endcode %}

优化器将会在查询表T1的时候选择索引扫描而在查询表T2的时候选用全表扫描。聚簇因子是帮助优化器作出决定的关键信息。由此可见,由于聚簇因子是一个与索引相关的统计信息,它通过査看表中数据块来进行计算。

> 计算索引的聚簇因子

{% code lineNumbers="true" %}
```sql
select 
    t.ename,t.blk_no,
    lag(blk_no,1,blk_no) over(order by t.ename) pre_blk_no,
    case when blk_no != lag(blk_no,1,blk_no) over(order by t.ename) or rownum = 1
    then '*** +1'
    else NULL 
    END cluf_ct
from (
    select e.ename,dbms_rowid.rowid_block_number(rowid) blk_no 
    from EMP e order by e.ename 
) t
```
{% endcode %}

聚簇因子是通过每次当前行的块编号与前一行的块编号不同时对计数器加一来进行计算的。在本例中，这样的情况发生了7次。这个数字表示了有7个不同的块用来存放这个表的数据。你可以从输出结果中看到，实际上仅有两个块包含数据(块编号84和88)。事实上，聚簇因子并不是十分准确的。

尽管大多数时候这种聚簇因子计算上的偏差不会引起足够大的差别以至于导致优化器错误地估计索引成本从而放弃使用索引，但的确有可能会出现这种情况。如果优化器没有选择你所希望使用的索引，它还有可能选择另一个能满足谓语的类似数据列上的索引。在这样的情况下，你就需要仔细分析所创建的索引，看看是否存在一种方法可以将几个索引合并为一个复合索引。不要错误地以为可以通过重建索引来“修正”聚簇因子。聚簇因子与表中数据而不是与索引相关。因此，重建索引对它没有任何影响。

另一方面，如果你开始考虑重建表来改进聚簇因子，你需要很小心地来进行。表一般都有多个索引。你不可能通过重建表来使其排序方式适合一个索引而不影响其他列上的索引。因此，重建可能帮助改进了一个索引却破坏了其他的索引。并且,重建表通常是非常耗费时间和资源的过程，因为你今天按照一定的顺序重建了表并不意味着随着时间的推移，数据行插入、更新或删除之后还能保持这样的顺序。

### 索引唯一扫描

当谓语中包含使用**`UNIQUE`**或**`PRIMARY KEY索引`**的列作为条件的时候就会选用索引唯一扫描。这种类型的索引能够保证对于某个特定的值只返回一行数据。在这种情况下,索引结构将会被从根到叶子进行遍历直到某个条目，取出其行编号，然后使用这个行编号来访问包含这一行的表数据块。计划中的**`TABLE ACCESS BY INDEX ROWID步骤`**表明了对于表数据块的访问。除非在某些特定的情况下例如数据行是链式的或者包含存储在别处的**`大对象(Large Object,LOB)`**，需要访问的数据块数总是等于索引的高度加一。下列代码给出了一个将会生成索引唯一扫描执行计划的查询例子。

> 索引唯一扫描

{% code lineNumbers="true" %}
```sql
SELECT * FROM EMPLOYEES E WHERE E.ID = 100;

执行计划
----------------------------------------------------------
Plan hash value: 1395705693
--------------------------------------------------------------------------------------------
| Id  | Operation                   | Name         | Rows  | Bytes | Cost (%CPU)| Time     |
--------------------------------------------------------------------------------------------
|   0 | SELECT STATEMENT            |              |     1 |    30 |     1   (0)| 00:00:01 |
|   1 |  TABLE ACCESS BY INDEX ROWID| EMPLOYEES    |     1 |    30 |     1   (0)| 00:00:01 |
|*  2 |   INDEX UNIQUE SCAN         | SYS_C0011531 |     1 |       |     1   (0)| 00:00:01 |
--------------------------------------------------------------------------------------------
Predicate Information (identified by operation id):
---------------------------------------------------
   2 - access("E"."ID"=100)
```
{% endcode %}

### 索引范围扫描

当谓语中包含将会返回一定范围数据的条件时就会选用索引范围扫描。**索引可以是唯一或者不唯一的**，因为是由该条件来确定是否会返回多个数据行的。所指定的条件可以使用诸如**`<、>、LIKE、BETWEEN`**甚至是**`=`**等运算符。为了能够选用索引范围扫描，范围需要相当仔细地进行选择。范围越大，就越有可能会选用全扫描运算来代替它。下列代码给出了一个将会进行索引范围扫描的查询例子。

> 索引范围扫描

{% code lineNumbers="true" %}
```sql
SELECT * FROM EMPLOYEES E WHERE E.DEPARTMENT_ID = 60;

执行计划
----------------------------------------------------------
Plan hash value: 2056577954
-------------------------------------------------------------------------------------------------    
| Id  | Operation                   | Name              | Rows  | Bytes | Cost (%CPU)| Time     |
-------------------------------------------------------------------------------------------------    
|   0 | SELECT STATEMENT            |                   |     2 |    86 |     3   (0)| 00:00:01 |    
|   1 |  TABLE ACCESS BY INDEX ROWID| EMPLOYEES         |     2 |    86 |     3   (0)| 00:00:01 |    
|*  2 |   INDEX RANGE SCAN          | EMP_DEPARTMENT_IX |     2 |       |     1   (0)| 00:00:01 |    
-------------------------------------------------------------------------------------------------    
Predicate Information (identified by operation id):
---------------------------------------------------
   2 - access("E"."DEPARTMENT_ID"=60)
```
{% endcode %}

范围扫描将会从根数据块开始到第一个包含符合特定条件的条目所在的叶子数据块来遍历索引结构。再从那一点开始，从索引条目中取出一个行编号然后取出相应的表数据块(通过索引行编号访问数据表)。在第一行被取出来之后，之前的叶子索引块将再一次被访问并读取下一个索引条目来获取下一个行编号。这种索引叶子块和表数据块之间的反复来回将会不断持续直到所有匹配的索引条目都被读出。**因此，所需访问数据块的次数将包括索引中的分支块数(可以通过索引的blevel统计信息得出)加上符合条件的索引条目数乘以2**。必须要乘以2是因为每取出表中的一行需要首先访问索引叶子块取出行编号然后通过该行编号访问表数据块。所以，如果返回5行数据并且blevel为3，则总的需要访问的数据块次数将是`(5行x2)+3=13`。

如果符合条件的条目范围足够大，则很可能需要访问不止一个叶子块。当这种情况发生时，所需的下一个叶子块可以通过使用存储在当前叶子块中的指向下一个叶子块的指针来读取(同样也有指向上一个叶子块的指针)。由于这些指针的存在，就没有必要再回到分支块来确定下一个块的位置。

当选择了索引范围扫描时，执行计划中的谓语信息将会示出用来访问索引的条件。在该例子中，计划中的第2步上有一个星号。这是一个表明在计划的下面列出了这一步的谓语信息的标志。在那儿，你会看到一个条目表明索引条目访问通过条件`DEPARTMENTID=60`来确认。

也有一些情况下你觉得应该使用索引范围扫描的谓语实际上没有使用。例如，如果你使用一个以通配符开头的条件如`'%abc’的LIKE运算符`，优化器将不会选用该列上的索引范围扫描，因为条件太宽泛了。另一个类似的情况就是，当你有一个使用`组合索引中非引导列的谓语时`。在这种情况下，将更可能选择使用索引跳跃扫描。

我想提醒你注意的索引范围扫描的最后一个精妙之处在于它能够使用一个升序排列的索引(默认值)来返回降序排列的数据行。即使全扫描可能更合理，优化器也有可能会选择使用索引来访问数据行。这种情况可能发生在查询中包含有索引列上的`0RDER BY`子句的时候。由于索引是按照顺序存储的，使用索引来读取数据也就意味着数据行是按照一定的顺序来获取的，可以避免再单独做一次排序的步骤。但是，如果`ORDER BY`子句要求按降序排列会怎样呢?因为索引是按升序来排列的，索引能够满足降序排列的要求吗?可以。下列代码清单给出了使用特殊的范围扫描来处理这种情况的一个例子。

> 使用索引范围扫描来避免排序

{% code lineNumbers="true" %}
```sql
SELECT * FROM EMPLOYEES E 
WHERE E.DEPARTMENT_ID IN (100,70) ORDER BY DEPARTMENT_ID DESC;

执行计划
----------------------------------------------------------
Plan hash value: 3707994525
---------------------------------------------------------------------------------------------------  
| Id  | Operation                     | Name              | Rows  | Bytes | Cost (%CPU)| Time     |  
---------------------------------------------------------------------------------------------------  
|   0 | SELECT STATEMENT              |                   |     2 |    86 |     2   (0)| 00:00:01 |  
|   1 |  INLIST ITERATOR              |                   |       |       |            |          |  
|   2 |   TABLE ACCESS BY INDEX ROWID | EMPLOYEES         |     2 |    86 |     2   (0)| 00:00:01 |  
|*  3 |    INDEX RANGE SCAN DESCENDING| EMP_DEPARTMENT_IX |     1 |       |     1   (0)| 00:00:01 |  
---------------------------------------------------------------------------------------------------  
Predicate Information (identified by operation id):
---------------------------------------------------
   3 - access("E"."DEPARTMENT_ID"=70 OR "E"."DEPARTMENT_ID"=100)
```
{% endcode %}

{% hint style="info" %}
在这个例子中，索引条目实际上被以相反的顺序读取，避免了再做单独的排序运算。
{% endhint %}

### 索引全扫描

在好几种情况下都将会选择索引全扫描，包括:当没有谓语但是所需获取列的列表可以通过其中一列的索引来获得，谓语中包含一个位于索引中非引导列上的条件，或者数据可以通过一个排过序的索引来获取并且会省去单独的排序步骤。下列代码清单针对这凡种情况分别给出了例子。

> 索引全扫描的例子

{% code lineNumbers="true" %}
```sql
--没有谓语但是可以通过查询列的索引来获得;该列需NOT NULL约束
SELECT EMAIL FROM EMPLOYEES E;

执行计划
----------------------------------------------------------
Plan hash value: 3860738740
----------------------------------------------------------------------------------------
| Id  | Operation        | Name                | Rows  | Bytes | Cost (%CPU)| Time     |
----------------------------------------------------------------------------------------
|   0 | SELECT STATEMENT |                     |     9 |   153 |     1   (0)| 00:00:01 |
|   1 |  INDEX FULL SCAN | EMPLOYEES_EMAIL_IDX |     9 |   153 |     1   (0)| 00:00:01 |
----------------------------------------------------------------------------------------
```
{% endcode %}

{% code lineNumbers="true" %}
```sql
--谓语中包含一个位于索引中非引导列上的条件
CREATE INDEX EMPLOYEES_DEPARTMENT_ID_IDX ON DEMO.EMPLOYEES (DEPARTMENT_ID,EMAIL);
SELECT E.EMAIL,E.DEPARTMENT_ID 
FROM EMPLOYEES E WHERE E.EMAIL LIKE '6%';

执行计划
----------------------------------------------------------
Plan hash value: 2903976476
------------------------------------------------------------------------------------------------     
| Id  | Operation        | Name                        | Rows  | Bytes | Cost (%CPU)| Time     |     
------------------------------------------------------------------------------------------------     
|   0 | SELECT STATEMENT |                             |     2 |    60 |     1   (0)| 00:00:01 |     
|*  1 |  INDEX FULL SCAN | EMPLOYEES_DEPARTMENT_ID_IDX |     2 |    60 |     1   (0)| 00:00:01 |     
------------------------------------------------------------------------------------------------     
Predicate Information (identified by operation id):
---------------------------------------------------
   1 - access("E"."EMAIL" LIKE '6%')
       filter("E"."EMAIL" LIKE '6%')
```
{% endcode %}

索引全扫描运算将会扫描索引结构中的每一个叶子块,读取每个条目的行编号,并取出数据行。每个叶子块都要被访问。这通常比全表扫描效率要高因为每一个索引块要比表数据块包含更多条目，从而总的需要访问的块数也就相应较少。在查询字段列表中所有字段都是索引条目的一部分的时候，就能够完全跳过访问表的步骤。这就意味着选用索引全扫描将比读取所有的表数据块效率更高。&#x20;

你可能从上个例子中已经注意到了索引全扫描运算也能够按降序来读取数据以避免进行单独的排序。对于索引全扫描还有另外一个最优方法。这发生在当需要查询某一列的最小或最大值而这一列又是索引列的时候。下列代码清单给出了这种运算选择的一个例子。

> 索引全扫描求最小/最大值的最优方法

{% code lineNumbers="true" %}
```sql
SELECT MIN(DEPARTMENT_ID) FROM EMPLOYEES;
执行计划
----------------------------------------------------------
Plan hash value: 1997243859
----------------------------------------------------------------------------------------------------------
| Id  | Operation                  | Name                        | Rows  | Bytes | Cost (%CPU)| Time     |
----------------------------------------------------------------------------------------------------------
|   0 | SELECT STATEMENT           |                             |     1 |    13 |     2   (0)| 00:00:01 |
|   1 |  SORT AGGREGATE            |                             |     1 |    13 |            |          |
|   2 |   INDEX FULL SCAN (MIN/MAX)| EMPLOYEES_DEPARTMENT_ID_IDX |     1 |    13 |     2   (0)| 00:00:01 |
----------------------------------------------------------------------------------------------------------

SELECT MAX(DEPARTMENT_ID) FROM EMPLOYEES;
执行计划
----------------------------------------------------------
Plan hash value: 1997243859
----------------------------------------------------------------------------------------------------------
| Id  | Operation                  | Name                        | Rows  | Bytes | Cost (%CPU)| Time     |
----------------------------------------------------------------------------------------------------------
|   0 | SELECT STATEMENT           |                             |     1 |    13 |     2   (0)| 00:00:01 |
|   1 |  SORT AGGREGATE            |                             |     1 |    13 |            |          |
|   2 |   INDEX FULL SCAN (MIN/MAX)| EMPLOYEES_DEPARTMENT_ID_IDX |     1 |    13 |     2   (0)| 00:00:01 |
----------------------------------------------------------------------------------------------------------
```
{% endcode %}

{% code title="优化同时查询最大最小值时" lineNumbers="true" %}
```sql
SELECT MIN(DEPARTMENT_ID),MAX(DEPARTMENT_ID) FROM EMPLOYEES;
执行计划
----------------------------------------------------------
Plan hash value: 1756381138
--------------------------------------------------------------------------------
| Id  | Operation          | Name      | Rows  | Bytes | Cost (%CPU)| Time     |
--------------------------------------------------------------------------------
|   0 | SELECT STATEMENT   |           |     1 |    13 |     5   (0)| 00:00:01 |
|   1 |  SORT AGGREGATE    |           |     1 |    13 |            |          |
|   2 |   TABLE ACCESS FULL| EMPLOYEES |  2738 | 35594 |     5   (0)| 00:00:01 |
--------------------------------------------------------------------------------

SELECT
    (SELECT MIN(DEPARTMENT_ID) FROM EMPLOYEES) MIN,
    (SELECT MAX(DEPARTMENT_ID) FROM EMPLOYEES) MAX
FROM DUAL;
执行计划
----------------------------------------------------------
Plan hash value: 4208130311
----------------------------------------------------------------------------------------------------------
| Id  | Operation                  | Name                        | Rows  | Bytes | Cost (%CPU)| Time     |
----------------------------------------------------------------------------------------------------------
|   0 | SELECT STATEMENT           |                             |     1 |       |     2   (0)| 00:00:01 |
|   1 |  SORT AGGREGATE            |                             |     1 |    13 |            |          |
|   2 |   INDEX FULL SCAN (MIN/MAX)| EMPLOYEES_DEPARTMENT_ID_IDX |     1 |    13 |     2   (0)| 00:00:01 |
|   3 |  SORT AGGREGATE            |                             |     1 |    13 |            |          |
|   4 |   INDEX FULL SCAN (MIN/MAX)| EMPLOYEES_DEPARTMENT_ID_IDX |     1 |    13 |     2   (0)| 00:00:01 |
|   5 |  FAST DUAL                 |                             |     1 |       |     2   (0)| 00:00:01 |
----------------------------------------------------------------------------------------------------------
```
{% endcode %}

如上例所示，当需要进行MIN或MAX聚合运算的时候，优化器能够选择一种特别优化的索引全扫描运算版本。在这样特别的情况下，当使用索引来快速获取最小值时，这个最小值将会是第一个索引叶子块中的第一个条目;当获取最大值时，将会是最后一个索引叶子块的最后一个条目。这非常有意义，因为索引是按照排序后的顺序来存储的，因此最大/最小值必然在第一个和最后一个叶子块中。但真正最妙之处在于，在这些特别的例子中索引全扫描并不是真正的全扫描—仅仅是对根块、一个或多个分支块以及第一个和最后一个叶子块的扫描。这也就是说找到最大/最小值在所需访问的块数目方面耗费的成本是很低的，速度也是很快的。尽管索引全扫描运算的名头可能让你觉得有点混乱，因为一般来说索引全扫描要读取所有的索引叶子块。这个优化对性能来说是一场精彩的胜利。

我确实也给出了一个在查询中同时包含MIN和MAX集合运算的例子，你也许注意到了，优化器选择了做全表扫描然后排序而不是非常有优势的索引全扫描运算。我想这是优化器处理这种情况的一个不足之处。有一个非常简单的办法可以让它选择具有优势的索引全扫描,就是把这两个查询分开来写。通过这种方法，你就可以体会到索引全扫描的好处了。

### 索引跳跃扫描

当谓语中包含位于索引中非引导列上的条件,并且引导列的值是唯一的时候会选择索引跳跃扫描。在早期的Oracle版本中，如果一个谓语使用了索引中的非引导列，则该索引不能被选用。在Oracle 9版本中这一情况通过引入索引跳跃扫描得到了改变。跳跃扫描通过逻辑上将多个列的索引分解为多个较小的子索引来实现。逻辑子索引的数目由索引引导列中唯一值的个数决定。因此，引导列中唯一值的数目越多，所需要建立的逻辑子索引也越多。如果需要建立的逻辑子索引太多，则有可能这个运算的效率还不如全表扫描高。但是，在需要的子索引数目较少的情况下,这个运算的效率通常要比全表扫描高,因为扫描较小索引块的效率要比扫描较大的表数据块效率高。下列代码清单给出了一个索引跳跃扫描的例子(注意:在这个例子中,我使用了hr.employees表的一个副本，数据行数大约为28000行)。

> 索引跳跃扫描的例子

<figure><img src="../.gitbook/assets/image (3) (1) (1) (1).png" alt=""><figcaption></figcaption></figure>

<figure><img src="../.gitbook/assets/image (2) (1) (1) (1).png" alt=""><figcaption></figcaption></figure>

在这个例子中，索引的引导列`job_id`具有19个唯一值。使用索引跳跃扫描来访问符合条件的23行数据`(first_name='william')`，将会有50次一致性读取(逻辑块访问)。如果使用全表扫描则需要访问376个数据块。由此可见，跳跃扫描的效率更高。索引跳跃扫描过程中所发生的事情就是索引被逻辑地划分为19个子索引，然后对每个子索引进行扫描寻找符合条件`firstname'William'`的。对于这种索引扫描类型，需要记住的一点是索引引导列的唯一值越少，所需的逻辑子索引数目也越少，从而需要访问的总块数也越少

### 索引快速全扫描

索引快速全扫描更像全表扫描而不像其他类型的索引扫描。当选用索引快速全扫描时，所有索引块都将通过多块读取来进行读取。**这种类型的索引扫描是用来在查询列表中所有字段都包含在索引中并且索引中至少有一列具有非空约束时替代全表扫描的。**在这种情况下,数据通过索引来进行访问而不必访问表数据块。与其他索引扫描类型不同，索引快速全扫描并不能用来避免排序，因为数据块是通过无序的多表读取来读取的。下列代码清单给出了一个索引快速全扫描查询计划的例子。

{% code lineNumbers="true" %}
```sql
SELECT DEPARTMENT_ID FROM EMPLOYEES ORDER BY DEPARTMENT_ID;
执行计划
----------------------------------------------------------
Plan hash value: 3447538987
--------------------------------------------------------------------------------
| Id  | Operation          | Name      | Rows  | Bytes | Cost (%CPU)| Time     |
--------------------------------------------------------------------------------
|   0 | SELECT STATEMENT   |           |  2738 | 35594 |     6  (17)| 00:00:01 |
|   1 |  SORT ORDER BY     |           |  2738 | 35594 |     6  (17)| 00:00:01 |
|   2 |   TABLE ACCESS FULL| EMPLOYEES |  2738 | 35594 |     5   (0)| 00:00:01 |
--------------------------------------------------------------------------------

ALTER TABLE EMPLOYEES MODIFY DEPARTMENT_ID NUMBER NOT NULL;
SELECT DEPARTMENT_ID FROM EMPLOYEES ORDER BY DEPARTMENT_ID;
执行计划
----------------------------------------------------------
Plan hash value: 1337987591
-----------------------------------------------------------------------------------------------------
| Id  | Operation             | Name                        | Rows  | Bytes | Cost (%CPU)| Time     |
-----------------------------------------------------------------------------------------------------
|   0 | SELECT STATEMENT      |                             |  2738 | 35594 |     5  (20)| 00:00:01 |
|   1 |  SORT ORDER BY        |                             |  2738 | 35594 |     5  (20)| 00:00:01 |
|   2 |   INDEX FAST FULL SCAN| EMPLOYEES_DEPARTMENT_ID_IDX |  2738 | 35594 |     4   (0)| 00:00:01 |
-----------------------------------------------------------------------------------------------------
```
{% endcode %}

{% hint style="info" %}
这个例子说明了索引快速全扫描运算为了被选择是如何依赖于非空约束的。如果没有这个约束，将会选择全表扫描运算。
{% endhint %}

## 联结方法

如果在你的查询中有多张表，在优化器确定了每个表最恰当的访问方法之后，下一步就是确定将这些表联结起来的最佳方法以及最恰当的顺序。任何时候当在FROM子句中有多个表时，就需要进行联结。表之间的关系通过WHERE子句中的一个条件来定义。如果没有指定任何条件，联结就会隐含地定义为一个表中的每一行将与另一个表中的所有行匹配。这被称为笛卡儿联结，我将在这一节稍后进行更详细的讨论。&#x20;

联结发生在一对表或者数据行源之间。当在FROM子句中存在多张表时，优化器将会决定哪种联结运算对于每一对表来说效率最高。**联结的方法有:嵌套循环联结、散列联结、排序-合并联结以及笛卡儿联结。**每种联结方法都有一定的最适合使用的条件。对于每对需要联结的表，优化器还必须确定表联结的顺序。下图示出了一个包括4张表的查询是如何来进行联结的。

<figure><img src="../.gitbook/assets/image (4) (1) (1) (1).png" alt=""><figcaption><p>联结顺序示例图</p></figcaption></figure>

注意在第一对表联结以后，下一张表是与第一个联结得到的结果行进行联结。在这次联结进行完之后，下一个联结还是与其结果行联结。这个过程会不断进行直到所有的表都被联结。

每个联结方法都有两个分支。所访问的`第一张表通常被称为驱动表(the driving table)`，访问的`第二张表则称为内层表或被驱表(inner或driven-to table)。`优化器通过使用统计信息和WHERE子句中的筛选条件计算每个表分别返回多少行数据来确定哪张表是驱动表。`预估大小最小(就块、数据行以及字节而言)的表通常被作为驱动表。`尤其是当优化器可以确定其中的一张表基于`UNIQUE或PRIMARY KEY约束`将最多返回一行的时候，这样的表在联结过程中将放在前面。使用外联结运算符的表必须放在所联结表的后面。除了这两种特殊情况以外，其他表的联结顺序都是由优化器使用所能得到的表、列以及索引统计信息计算得到的选择比来进行评估的。

### 嵌套循环联结

嵌套循环联结使用一次访问运算所得到的结果集中的每一行来与另一个表进行对碰。如果结果集的大小是有限的并且在用来联结的列上建有索引的话,这种联结的效率通常是最高的。嵌套循环联结的运算成本主要是读取外层表中的每一行并将其与所匹配的内层表中的行联结所需的成本。

顾名思义，嵌套循环联结就是一个循环嵌在另一个循环当中。外层循环基本来说就是一个只使用WHERE子句中的属于驱动表的条件对它进行的查询。当数据行经过了外层条件选并被确认匹配条件后，这些行就会逐个进入到内层循环中。然后再基于联结列进行逐行检查看是否与被联结的表中的某一行相匹配。如果这一行与第二次的检查相匹配，就将会被传递到查询计划的下一步或者如果没有更多步骤的话直接被包含在最终的结果集中。

**这种类型的联结的强大之处在于所使用的内存是非常少的。**因为数据行集一次只加工一行，所需的开支也是非常小的。由于这个原因，除了使用一次加工一行这种方式来建立一个很大的数据集需要较长时间这一点以外，它也是适合进行大数据集加工的。这就是为什么前面我提过的嵌套循环联结在结果集较小的时候会是最好的。嵌套循环联结的基本度量就是为了准备最终结果集所需要访问的数据块数目。

让我们来看一个简单的查询例子，并将其分解为嵌套循环联结是如何进行处理的。

{% code lineNumbers="true" %}
```sql
SELECT EMPNO,ENAME,DNAME,LOC
FROM EMP,DEPT
WHERE EMP.DEPTNO = DEPT.DEPTNO 
```
{% endcode %}

这个查询将会被像下面这段伪码一样来进行处理:

{% code lineNumbers="true" %}
```sql
FOR EACH ROW IN (SELECT empno,ename,deptno from emp) LOOP
    FOR (SELECT dname,loc from dept where deptno=outer.deptno) LOOP
        IF MATCH THEN 把这一行传递到下一步
        IF INNER JOIN AND NO MATCH THEN 丢弃这一行
        IF OUTER JOIN AND NO MATCH THEN 将INNER列的值设置为空，并将该行传递到下一步
    END LOOP
END LOOP
```
{% endcode %}

> 嵌套回路

{% code lineNumbers="true" %}
```sql
SELECT EMPNO,ENAME,LOC
FROM SCOTT.EMP,SCOTT.DEPT
WHERE EMP.DEPTNO=DEPT.DEPTNO AND EMP.EMPNO = '7369';

PLAN_TABLE_OUTPUT                                                                       |
----------------------------------------------------------------------------------------+
Plan hash value: 2385808155                                                             |
----------------------------------------------------------------------------------------|
| Id  | Operation                    | Name    | Rows  | Bytes | Cost (%CPU)| Time     ||
----------------------------------------------------------------------------------------|
|   0 | SELECT STATEMENT             |         |     1 |    24 |     2   (0)| 00:00:01 ||
|   1 |  NESTED LOOPS                |         |     1 |    24 |     2   (0)| 00:00:01 ||
|   2 |   TABLE ACCESS BY INDEX ROWID| EMP     |     1 |    13 |     1   (0)| 00:00:01 ||
|*  3 |    INDEX UNIQUE SCAN         | PK_EMP  |     1 |       |     0   (0)| 00:00:01 ||
|   4 |   TABLE ACCESS BY INDEX ROWID| DEPT    |     4 |    44 |     1   (0)| 00:00:01 ||
|*  5 |    INDEX UNIQUE SCAN         | PK_DEPT |     1 |       |     0   (0)| 00:00:01 ||
----------------------------------------------------------------------------------------|
Predicate Information (identified by operation id):                                     |
---------------------------------------------------                                     |
   3 - access("EMP"."EMPNO"=7369)                                                       |
   5 - access("EMP"."DEPTNO"="DEPT"."DEPTNO")                                           |
```
{% endcode %}

执行计划展示了将emp表作为驱动表，dept表作为内层(或被驱)表的套循环联结方法。在这个嵌套循环执行计划中，`NESTED_LOOPS`运算后所列出的第一个表是驱动表，将会采用为它选定的方法来进行访问。在本例中，将会对emp表进行全表扫描。也就是说emp表中的所有块都将通过多块读取来读出，然后再逐行进行访问，并将deptmo(联结列)传递给内层循环针对dept表的查询。对于内联结，每一行在dept表的deptmo列有匹配值的数据行都将被返回。对于外联结emp表中的每一行都将被返回，dept表中的列将用null值来填充。

如果你想搞清楚为什么emp表会被选为驱动表，那么让我们花几秒钟来仔细看看这个查询。该查询是要找到所有在两个表中的deptno列相匹配的数据行。在我的测试中，emp表在deptno这列上没有索引，因此要访问它的唯一的方法就是进行全表扫描。由于嵌套循环联结的实现方式就是对外层表的每一行进行内联结，如果将dept表选为驱动表，对于dept表中的每一行都要在cmp表中进行全表扫描。另一方面，使用emp表来进行驱动就只需要进行一次全表扫描，并且由于在dept表中的deptno列上建有索引(它是主键)，内层循环可以从dept表中直接访问数据行。下列代码所示为这两种联结顺序的自动追踪统计信息输出的比较。

> 强制优化器选择将dept作为驱动表

{% code lineNumbers="true" %}
```sql
SELECT EMPNO,ENAME,LOC
FROM SCOTT.EMP,SCOTT.DEPT
WHERE EMP.DEPTNO=DEPT.DEPTNO AND EMP.EMPNO = '7369';
```
{% endcode %}

### 排序合并联结

排序-合并联结独立地读取需要联结的两张表，对每张表中的数据行(仅是那些满足WHERE子句中条件的数据行)按照联结键进行排序，然后对排序后的数据行集进行合并。对这种联结方法来说排序的开销是非常大的。对于不能够放入内存中的大的数据源来说，可能会使用临时磁盘空间来完成排序。这是非常耗占内存和时间资源的。但是一但数据行集排序完成了，合并的过程是非常快的。为了进行合并，数据库轮流操作两个列表，比较最上面的数据行，丢弃在排序队列中比另一列表中的最上面一行出现得早的数据行，并只返回匹配的行。&#x20;

让我们来使用与上一个例子中相同的查询，并具体分析排序-合并联结是如何进行的。

{% code lineNumbers="true" %}
```sql
SELECT EMPNO,ENAME,LOC
FROM SCOTT.EMP,SCOTT.DEPT
WHERE EMP.DEPTNO=DEPT.DEPTNO
```
{% endcode %}

这个查询将会被像下面这段伪码一样来进行处理:

{% code lineNumbers="true" %}
```sql
select empno,ename,deptno from emp order by deptno
select dname,loc deptno from dept order by deptno
--比较行集和返回行的deptno在两个列表匹配中
--为另一个表设置为null的列值
compare the rowsets and return rows where deptno in both lists match
setting column values for the other table to null
```
{% endcode %}

> 排序-合并联结

{% code lineNumbers="true" %}
```sql
SELECT /*+ ordered */ EMPNO,ENAME,DNAME,LOC
FROM SCOTT.EMP,SCOTT.DEPT
WHERE EMP.DEPTNO=DEPT.DEPTNO;

PLAN_TABLE_OUTPUT                                                                       |
----------------------------------------------------------------------------------------+
Plan hash value: 844388907                                                              |
----------------------------------------------------------------------------------------|
| Id  | Operation                    | Name    | Rows  | Bytes | Cost (%CPU)| Time     ||
----------------------------------------------------------------------------------------|
|   0 | SELECT STATEMENT             |         |    14 |   462 |     6  (17)| 00:00:01 ||
|   1 |  MERGE JOIN                  |         |    14 |   462 |     6  (17)| 00:00:01 ||
|   2 |   TABLE ACCESS BY INDEX ROWID| DEPT    |     4 |    80 |     2   (0)| 00:00:01 ||
|   3 |    INDEX FULL SCAN           | PK_DEPT |     4 |       |     1   (0)| 00:00:01 ||
|*  4 |   SORT JOIN                  |         |    14 |   182 |     4  (25)| 00:00:01 ||
|   5 |    TABLE ACCESS FULL         | EMP     |    14 |   182 |     3   (0)| 00:00:01 ||
----------------------------------------------------------------------------------------|
Predicate Information (identified by operation id):                                     |
---------------------------------------------------                                     |
   4 - access("EMP"."DEPTNO"="DEPT"."DEPTNO")                                           |
       filter("EMP"."DEPTNO"="DEPT"."DEPTNO")                                           |
```
{% endcode %}

我使用了和前面一样的查询但必须使用ordered提示来强制选择执行计划。请注意在执行计划运算中是如何体现对dept表进行合并联结然后访问索引，以及对emp表进行全表扫描实现排序联结运算的。首先要关注的就是对dept表所使用的索引扫描。在这个例子中，因为索引将按排序后的顺序返回数据，优化器选择使用索引来读取表数据。这就意味着可以避免一次单独的排序运算。对于emp表则必须进行全表扫描然后要单独进行排序，因为在deptno这一列上没有索引可用。在两个数据行集都准备好并排序后，它们将会被合并到一起。

排序-合并联结将会访问所需的数据块然后在内存中(或者如果没有足够内存的话使用临时磁盘空间)进行排序和合并。因此，当你对排序-合并联结和嵌套循环联结进行逻辑读取比较的时候，尤其是对于一个更大的数据行源的查询，你可能会发现嵌套循环联结所需的块访问更多。这是否意味着排序-合并联结就是更好的选择呢?不一定。你必须把所有需要进行排序以及合并等步骤的工作都考虑进去，并意识到这些工作最终有可能比做较多的块访问花费的时间更多。

排序-合并联结一般最适合于数据筛选条件有限并返回有限数据行的查询。如果没有可用的更直接访问数据的索引时排序-合并联结也通常是较好的选择。总的来说，在条件为非等式的时候排序-合并联结通常是最好的选择。例如，`WHERE table1.column1 between table2.column1 and table2.column2`这样的联结条件就较适合排序一合并联结。在下一节中你将看到,在这种情况下进行散列联结是不可能的。如果数据行源非常大，排序-合并就可能是唯一可行的选择。

### 散列联结

散列联结，与排序-合并联结类似，首先应用WHERE子句中的筛选标准来独立地读取要进行联结的两个表。基于表和索引的统计信息，被确定为返回最少行数的表被完全散列化到内存中。这个散列表包含了原表的所有数据行并被基于将联结键转化为散列值的随机函数载入到散列桶中只要有足够的内存空间，这个散列表将一直放在内存中。然而，如果没有足够的内存，散列表将会被写入到临时磁盘空间。

下一步就是读取另一张较大的表并对联结键列应用散列函数。然后利用得到的散列值对较/的在内存中的散列表进行探测以寻找匹配的第一个表的行数据所在的散列桶。每个散列桶都有-个放在其中的数据行列表(通过一个位图来表示)。这个列表被用来与探测行进行匹配。如果四配成功，则返回这一行数据，否则丢弃。较大的表只读取一次,并检查其中的每一行来寻找匹配这与嵌套循环联结的不同之处在于此处内层表被多次读取。因此事实上在这个例子中，较大的表是驱动表，因此仅读取一次，而较小的散列表则被探测很多次。与嵌套回路联结执行计划不同在执行计划的输出中较小的散列表放在前面而较大的探测表放在后面。

使用与之前例子中相同的查询来进行分解，看看散列联结是如何进行的。

{% code lineNumbers="true" %}
```sql
SELECT EMPNO,ENAME,LOC
FROM SCOTT.EMP,SCOTT.DEPT
WHERE EMP.DEPTNO=DEPT.DEPTNO
```
{% endcode %}

{% code lineNumbers="true" %}
```sql
determine the smaller row set, or in the case of an outer join,
    use the outer joined table
select dname ,loc ,deptno from dept
hash the deptno column and build a hash table
select empno,ename,deptno from emp
hash the deptno column and probe the hash table
--检查位图以确认行匹配
if match made ,check bitmap to confirm row match
--丢弃这一行
if no match made,discard the row
```
{% endcode %}

> 散列联结

{% code lineNumbers="true" %}
```sql
---------------------------------------------------------------------------|
| Id  | Operation          | Name | Rows  | Bytes | Cost (%CPU)| Time     ||
---------------------------------------------------------------------------|
|   0 | SELECT STATEMENT   |      |    14 |   462 |     7  (15)| 00:00:01 ||
|*  1 |  HASH JOIN         |      |    14 |   462 |     7  (15)| 00:00:01 ||
|   2 |   TABLE ACCESS FULL| DEPT |     4 |   182 |     3   (0)| 00:00:01 ||
|   3 |   TABLE ACCESS FULL| EMP  |    14 |    80 |     3   (0)| 00:00:01 ||
---------------------------------------------------------------------------|
Predicate Information (identified by operation id):                        |
---------------------------------------------------                        |
   1 - access("EMP"."DEPTNO"="DEPT"."DEPTNO")                              |
```
{% endcode %}

在散列联结的执行计划中，较小的散列表列在前面而探测表列在后面。记住一点就是决定哪个表是最小的不仅取决于数据行数还取决于这些行的大小,因为整个行都必须要存放在散列表中。

当数据行源较大并且结果集也较大的情况下将更倾向于考虑散列联结。或者，如果要联结的其中一张表确定总是返回同一数据行源，也很可能会选用散列联结因为这样仅访问一次这张表。如果在这种情况下选用嵌套循环联结,这个数据行源就会被一遍又一遍地访问，需要比单独访问一次多做很多工作。最后，如果较小的表可以放到内存中，散列联结也会很受欢迎。

散列联结中访问数据块的方式类似于排序-合并联结中的访问方式。建立散列表所需的数据块将被读取，然后剩下的工作将会针对存放在内存中(如果没有足够内存的话将会是临时磁盘空间)的散列数据来进行。因此，当你对散列联结和排序-合并联结的逻辑读取进行比较的时候，访问的数据块数几乎是相等的。但相比于嵌套循环联结来说逻辑读取会比较少，因为数据块仅被读取了一次，然后或者存放到内存中(对于散列表)再进行访或者只读取一次(对于探测表)。

散列联结只有在相等联结的情况下才能进行。如前面所提到的，排序-合并联结可以用来处理特定的非等式的条件。散列联结只有在相等联结时才能选用的原因在于匹配是针对散列值来进行的，而在一个范围内来考虑散列值是没有意义的。下列代码给出了计算出的散列值并不必然对应于散列键值(在本例中，指的是它的数值)的一个例子。

{% code lineNumbers="true" %}
```sql
SELECT DISTINCT DEPTNO, ORA_HASH(DEPTNO,1000) HV
FROM SCOTT.EMP
ORDER BY DEPTNO;
DEPTNO|HV |
------+---+
    10|547|
    20|486|
    30|613|
    
SELECT DEPTNO FROM (
	SELECT DISTINCT DEPTNO, ORA_HASH(DEPTNO,1000) HV
	FROM SCOTT.EMP
	ORDER BY DEPTNO
) WHERE HV BETWEEN 100 AND 500;
DEPTNO|
------+
    20|

SELECT DISTINCT DEPTNO, ORA_HASH(DEPTNO,1000,50) HV
FROM SCOTT.EMP
ORDER BY DEPTNO;
DEPTNO|HV |
------+---+
    10|839|
    20|850|
    30|290|

SELECT DEPTNO FROM (
	SELECT DISTINCT DEPTNO, ORA_HASH(DEPTNO,1000,50) HV
	FROM SCOTT.EMP
	ORDER BY DEPTNO
) WHERE HV BETWEEN 100 AND 500;
DEPTNO|
------+
    30|
```
{% endcode %}

我用`ora_hash函数`来说明散列值是如何生成的。`ora_hash函数`有3个参数:一个任何基本类型的输入,最大散列桶值(最小值为0),以及一个种子值(默认值也是0)。因此,例如ora \_hash(10.1000)将会返回一个0\~1000之间的整数值。

在这两个例子中，我在第一个中使用了默认种子而在第二个中种子值为50。注意每一个deptno的散列值在各个查询中是非常不同的。因此当我想分别查询一定范围内的散列值时，我得到了不同的结果。但是，在这两种情况下，如果我只是查询一定范围的列值，我可以很容易得出想要的结果，并且可以保证总能得到正确的答案。这个例子是有点强制性质的，但我想给你一个对散列值对比比较感性的认识以便你可以很好地来理解为什么它们不适用于非等式联结。

### 笛卡尔联结

笛卡儿联结发生在当一张表中的所有行与另一张表中的所有行联结的时候。因此，这种联结所得到的结果集的总行数等于一张表(A)中的数据行数乘以另一张表(B)中的数据行数，也就是AxB=结果集中总的数据行数。通常当联结条件被忽略或忽视以致没有指定的联结列，所能做的唯一可能的运算就是将一张表中的所有内容与另一张表的所有内容联结起来的时侯会进行笛卡儿联结。

让我们继续使用前面使用过的查询，但是去掉WHERE子句，来具体看看笛卡儿联结是如何进行的。

{% code lineNumbers="true" %}
```sql
select empno,ename,dname,loc from emp,dept
```
{% endcode %}

这个查询将会被像下面这段伪代码一样来进行处理:

{% code lineNumbers="true" %}
```sql
determine the smaller table
select dname, loc from dept
select empno ename from emp
for each row in dept match it to every row in emp retaining all rows
```
{% endcode %}

> 笛卡儿积

{% code lineNumbers="true" %}
```sql
SELECT EMPNO,ENAME,DNAME,LOC
FROM SCOTT.EMP,SCOTT.DEPT

PLAN_TABLE_OUTPUT                                                            |
-----------------------------------------------------------------------------+
Plan hash value: 2034389985                                                  |
-----------------------------------------------------------------------------|
| Id  | Operation            | Name | Rows  | Bytes | Cost (%CPU)| Time     ||
-----------------------------------------------------------------------------|
|   0 | SELECT STATEMENT     |      |    56 |  1568 |    10   (0)| 00:00:01 ||
|   1 |  MERGE JOIN CARTESIAN|      |    56 |  1568 |    10   (0)| 00:00:01 ||
|   2 |   TABLE ACCESS FULL  | DEPT |     4 |    72 |     3   (0)| 00:00:01 ||
|   3 |   BUFFER SORT        |      |    14 |   140 |     7   (0)| 00:00:01 ||
|   4 |    TABLE ACCESS FULL | EMP  |    14 |   140 |     2   (0)| 00:00:01 ||
-----------------------------------------------------------------------------|
```
{% endcode %}

注意执行计划中所估算的数据行数以及最终数据行估计数是通过两张表中数据行数的乘积(4x14=56)得到的。从本例中你最终得到的是一个包含比你想要得到的多得多的数据行的结果集。当写SQL语句而没有正确检查执行计划时，笛卡儿联结可能会导致得到有很多重复行的结果集。并且，很多人遇到这种情况时首先做的事情是在SQL语句中增加distinct运算符。这样做可以去掉重复的行以得到正确的结果集，但是需要付出极高的成本。首先在这个地方就不应该出现重复值，但是既然它们已经存在了，增加distinct运算符将会导致先进行排序，然后去除掉所有重复行。这其实是做了很多无用功。因此，如果你在查询结果集中得到了不希望有的重复行请一定先检查笛卡儿联结的执行计划，而不是仅仅在查询中加个distinct了事。

关于笛卡儿联结执行计划你需要注意的一点就是BUFFERSORT运算的出现。这并不是一次真正的排序，而是由于Oracle将每一行与每一行进行联结，使用缓冲区排序机理来将第二张表的数据块从缓冲区缓存中复制出并放入到私有内存中,避免缓冲区缓存中的同一个数据块被一次又一次访问。这些重复访问需要更大量的逻辑读取，并且也会增加更多对缓冲区缓存中的这些数据块进行争夺的几率。因此，将这些数据块缓存到一个私有内存区域可能是一种更有效的实现重复联结的方式。

### 外联结

外联结返回一张表的所有行以及另一张联结表中满足联结条件的行数据。Oracle使用+字符来表明进行外联结。+号放在一对圆括号中，位于只有匹配才会返回数据行的表联结条件旁。正如我在对各种联结方法进行总述的时候指出的，外联结需要外联结表作为驱动表。这意味着有可能不能选用更加优化的联结执行顺序。因此，使用外联结的时候需要格外小心，因为它的选用有可能会影响到整个执行计划的性能。

> 外联结

在这个例子中，需要对总下单金额在S0和$5000之间的顾客进行计数。

<figure><img src="../.gitbook/assets/image (6) (1) (1) (1).png" alt=""><figcaption></figcaption></figure>

<figure><img src="../.gitbook/assets/image (5) (1) (1) (1).png" alt=""><figcaption></figcaption></figure>

当使用`Oracle的(+)运算符`时，会有一些在使用`ANSI语法`时没有的局限性。如果你尝试将同一个表与另外多张表进行外联结Oracle将会抛出一个错误。错误信息是:“ORA-01417:一张表最多只能与另外的一张表进行外联结"。而使用ANSI语法对表的数目没有限制，即使是一张表也可以外联结。Oracle外联结语法的另一个局限性在于它不支持全外联结。全外联结将会从左到右以及从右到左对两个表进行联结。

## 小结

在确定SQL语句的执行计划的时候，优化器必须做出几个关键的选择。首先，要确定查询中所用到的每个表最合适的访问方法。基本上有两种选择:索引扫描或全表扫描。每种访问方法用来访问包含SQL语句所需数据的实现方式是不同的。一旦优化器选定了访问方法，就必须选定联结方法。表将会被逐对进行联结，前一次联结的结果的数据行源被用来与下一个表进行联结，直到所有表都被联结并获得最终的结果集。

理解每种访问和联结方法是如何实现的可以有助于你写出可以让优化器作出最高效选择的SQL语句。能够复查执行计划、理解所选的运算以及这些运算是如何进行的也将有助于你来注意哪里是性能最有可能发生问题的地方。再说一次,理解表面之下的内部原理可以帮助你写出更好更快的SQL查询语句。

[^1]: 这是SQL\*Plus和PL/SQL中用于执行命令的关键字。它是"EXECUTE"的简写形式

[^2]: 这是调用Oracle数据库中的DBMS\_STATS包中的GATHER\_TABLE\_STATS 过程，用于收集表的统计信息，这些统计信息包括行数、列的分布情况、索引情况等

[^3]: 用于获取当前用户的名称，作为参数传递给 GATHER\_TABLE\_STATS 过程，表示收集当前用户的表的统计信息

[^4]: 收集统计信息的表的名称

[^5]: 这是收集统计信息的选项之一，指定了收集所有列的统计信息，并且为每列指定了采样大小为 1。这意味着对于每个列，只会采样一个值来获取统计信息。这种方法适用于大多数情况，因为它能够提供相对较好的性能和准确性

[^6]: 这个选项表示要对从属于指定表的所有索引、分区和相关对象也进行统计信息收集。这样做可以确保在优化查询时考虑到所有相关对象的统计信息
